# The TCM-only SoC has one generated clock.  Reset is asynchronously asserted
# and synchronously released by the ASYNC_REG-marked reset synchronizer; no
# functional data-path false paths are required.
